# local DB-pull cache -- regenerable, potentially large
branch_tracking/data/raw/

# same-run diff copies (git history is the real archive)
branch_tracking/output/*.prev

# python
__pycache__/
*.pyc
.venv/

# git worktrees live here (linked worktrees of this repo; not tracked by master)
.claude/worktrees/

# agent/execution scratch dirs (leak into the tree; never tracked)
.superpowers/
.claire/
