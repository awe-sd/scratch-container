[project]
name = "scratch-workspace"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "netcdf4>=1.7.4",
    "openpyxl>=3.1.5",
    "pillow>=12.3.0",
    "xarray>=2026.7.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
